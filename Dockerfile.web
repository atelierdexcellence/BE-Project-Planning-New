# Dockerfile para el servicio web (aplicación React con Vite)

# --- Etapa de Construcción (Build Stage) ---
FROM node:20-alpine AS builder

WORKDIR /app

# Copia package.json y package-lock.json para instalar dependencias
COPY package*.json ./

# Instala las dependencias
RUN npm install

# Copia el resto del código fuente
COPY . .

# Construye la aplicación Vite
RUN npm run build

# --- Etapa de Ejecución (Runtime Stage) ---
FROM nginx:alpine

# Copia la configuración de Nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copia los archivos estáticos construidos desde la etapa de construcción
COPY --from=builder /app/dist /usr/share/nginx/html

# Expone el puerto 80
EXPOSE 80

# Comando para iniciar Nginx
CMD ["nginx", "-g", "daemon off;"]
